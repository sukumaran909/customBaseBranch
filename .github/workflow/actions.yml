name: Secret testing 

on:
  push

jobs:
  first-job:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checking the content of Secret...
      run: echo ${{SECRETS.minato1}}
      run: echo "Exiting...."
