name: Secret testing 

on:
  push

jobs:
  first-job:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checking the content of Secret...
      env:
        value1: ${{ secrets.MINATO1 }}
      if: env.value1 == 'minato'
      run: "Found the key"
